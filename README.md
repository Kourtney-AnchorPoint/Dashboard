# VEGA Migration Workspace

Shared workspace for migrating VEGA off Base44 and versioning each step.

## Current Goal

Move VEGA into a codebase we control, with Git tracking the history so changes can be reviewed, reverted, and branched safely.
